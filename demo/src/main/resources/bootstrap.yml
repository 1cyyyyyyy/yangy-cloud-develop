server:
  servlet:
    context-path: /

spring:
  application:
    name: demo
  cloud:
    nacos:
      discovery:
        enabled: true
        service: ${spring.application.name}
      config:
        enabled: true
        encode: UTF-8
        file-extension: yml
